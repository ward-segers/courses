<table>
    <thead>
        <th>Commando</th>
        <th>Beschrijving</th>
    </thead>
    <tbody>
<tr>
<td>

```console
Switch>
```

</td>
<td>Standaard prompt bij het openen van een switch</td>
</tr>    
<tr>
<td>

```console
Switch> ?
```

</td>
<td>Toont alle mogelijke commando's</td>
</tr>
<tr>
<td>

```console
Switch> enable
```

</td>
<td>Schakelen naar een privileged user</td>
</tr>

<tr>
<td>

```console
Switch# show running-config
```

</td>
<td>Toont de huidige configuratie waarmee het toestel draait.(opgeslagen in RAM)</td>
</tr>
<tr>
<td>

```console
Switch# copy running-config startup-config
```

</td>
<td>Kopieert de running config naar de startup config</td>
</tr>
<tr>
<td>

```console
Switch# show startup-config
```

</td>
<td>Toont de opgeslagen configuratie waarmee het toestel draait.</td>
</tr>
<tr>
<td>

```console
Switch# disable
```

</td>
<td>Terugschakelen van privilege exec mode naar gebruiker exec mode</td>
</tr>
<tr>
<td>

```console
Switch# configure terminal
```

</td>
<td>Omschakelen naar global configuration mode</td>
</tr>
<tr>
<td>

```console
Switch(config)# exit
```

</td>
<td>terugkeren naar 1 niveau lager</td>
</tr>
<tr>
<td>

```console
Switch(config)# end
```

</td>
<td>terugkeren naar privilege exec mode (ook via CTRL + Z)</td>
</tr>
<tr>
<td>

```console
Switch(config)# hostname <SwitchNaam>
```

</td>
<td>Wijzigt de naam van een switch</td>
</tr>
<tr>
<td>

```console
S(config)# interface FastEthernet 0/1
```

</td>
<td>Gaat in interface configuratie mode op interface FastEthernet 0/1</td>
</tr>
<tr>
<td>

```console
S(config)# line console 0
```

</td>
<td>Gaat in line configuratie mode op console kabel 0</td>
</tr>
<tr>
<td>

```console
S(config)# line vty 0-15
```

</td>
<td>Gaat in line configuratie mode voor de vty lijnen 0 tot en met 15</td>
</tr>
<tr>
<td>

```console
S(config)# enable secret ditIsMijnPassw
```

</td>
<td>Stelt een geencrypteerd passwoord in (= ditIsMijnPassw) op enable (privileged user)</td>
</tr>
<tr>
<td>

```console
S(config)# line console 0
S(config-line)# password ditIsMijnPassw
S(config-line)# login
```

</td>
<td>Stelt een zichtbaar passwoord in (= ditIsMijnPassw) op  een communicatielijn (console)</td>
</tr>

<tr>
<td>

```console
S(config)# service password-encryption
```

</td>
<td>Encrypteerd alle ingestelde passwords. (nieuwe passwoorden niet, moeten opnieuw gebeuren)</td>
</tr>
<tr>
<td>

```console
S# show ip <interface>
```

</td>
<td>Toont ip informatie van de interfaces</td>
</tr>
<tr>
<td>

```console
S# show interfaces
```

</td>
<td>Toont details van de interfaces</td>
</tr>
<tr>
<td>

```console
S# show ip interface brief
```

</td>
<td>Toont beknopte samenvatting van de interfaces</td>
</tr>
<tr>
<td>

```console
S# reload
```

</td>
<td>Leest startup config opnieuw in</td>
</tr>
<tr>
<td>

```console
S# erase startup-config
```

</td>
<td>Startup config verwijderen</td>
</tr>

<tr>
<td>

```console
S(config)# interface vlan 1
S(config-if)# ip address 10.0.0.1 255.255.0.0
S(config-if)# no shutdown
```

</td>
<td>Instellen van een ip adres voor de switch. No shutdown zorgt ervoor dat de switch aan zal staan</td>
</tr>

<tr>
<td>

```console
S(config)# ip default-gateway 10.0.0.9
```

</td>
<td>Instellen van de default gateway</td>
</tr>
<tr>
<td>

```console
S(config)# banner motd #mijn banner#
```

</td>
<td>Instellen message of the day</td>
</tr>
<tr>
<td>

```console
S(config-interface)# description ip address for conf
```

</td>
<td>Geeft een beschrijving aan de interface</td>
</tr>
<tr>
<td>

```console
S(config)# username root password root
```

</td>
<td>instellen van gebruiker root met passwoord root</td>
</tr>
<tr>
<td>

```console
S(config-line)# transport OPTIONS
    OPTIONS:
        <input/output>
        <all/none/ssh/telnet>
```

</td>
<td>instellen uitgaand/inkomend verkeer voor verschillende protocollen</td>
</tr>
<tr>
<td>

```console
S(config)# interface gigabitethernet 0/0/0
S(config-if)# ipv6 address 2001:db8:acad:1::1/64
S(config-if)#no shutdown
```

</td>
<td>instellen van een GUA ipv6 adres en de interface inschakelen</td>
</tr>
<tr>
<td>

```console
S(config)# interface gigabitethernet 0/0/0
S(config-if)# ipv6 address fe80::1:1 link-local
S(config-if)#no shutdown
```

</td>
<td>instellen van een LLA ipv6 adres en de interface inschakelen</td>
</tr>
<tr>
<td>

```console
S(config)# ipv6 unicast-routing
```

</td>
<td>Schakel ipv6 op de router in</td>
</tr>
<tr>
<td>

```console
S# show cdp neighbors detail
```

</td>
<td>Toont het ip adres van elk 'buurtoestel"</td>
</tr>
<tr>
<td>

```console
S# debug
```

</td>
<td>Commando gebruikt om te debuggen (log berichten lokaal weergeven)</td>
</tr>
<tr>
<td>

```console
S# undebug all
```

</td>
<td>Stoppen met debuggen</td>
</tr>
<tr>
<td>

```console
S# terminal monitor
```

</td>
<td>Commando gebruikt om te weergeven van log berichten (remote)</td>
</tr>
<tr>
<td>

```console
S# terminal no monitor
```

</td>
<td>Stoppen met het weergeven van logs</td>
</tr>












<tr>
<td>

`mdix auto`

</td>
<td>inschakkelen van auto-mdix (in interface configuratie)</td>
</tr>
<tr>
<td>

`route print`

</td>
<td>Printen pc routering tabellen</td>
</tr>
<tr>
<td>

`netstat -r`

</td>
<td>Printen pc routering tabellen</td>
</tr>
<td>

`netstat`

</td>
<td>Geeft open verbinding weer, en welk protocol gebruikt wordt</td>
</tr>
<tr>
<td>

`show ip route`

</td>
<td>Toont de router zijn IPv4 routerings tabel</td>
</tr>
<tr>
<td>

`show ip arp`

</td>
<td>Toont de arp tabel op cisco switches</td>
</tr>
<tr>
<td>

`arp -a`

</td>
<td>Toont de arp tabel op windows pc</td>
</tr>
<tr>
<td>

`nslookup`

</td>
<td>opent prompt om een dns lookup te doen</td>
</tr>
<tr>
<td>

`ipconfig /release`

</td>
<td>ip adress geconfigureerd door dhcp vrijgeven</td>
</tr>
<tr>
<td>

`ipconfig /renew`

</td>
<td>Host nieuw ip adres doen verkregen via dhcp</td>
</tr>
<tr>
<td>

`ipconfig /displaydns`

</td>
<td>DNS entries op windows pc weergeven</td>
</tr>

</tbody>
</table>